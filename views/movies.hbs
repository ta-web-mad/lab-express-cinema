<div class="movies-list">
    {{#each allMovies}}
    <div class="movie-card">
        <img src="{{image}}" alt="{{title}}">
        <div class="movie-info">
            <h1>{{title}}</h1>
            <a href="">See more</a>
        </div>
    </div>
    {{/each}}
</div>