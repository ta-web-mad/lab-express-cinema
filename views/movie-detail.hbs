<div>
    <img src="{{image}}" alt="{{title}}">
    <div>
        <a href="/movies">Go back</a>
        <h1>{{title}}</h1>
        <h2>Director: {{director}}</h2>
        <ul>Stars
            {{#each stars}}
            <li>{{this}}</li>
            {{/each}}
        </ul>
        <p>{{description}}</p>
        <div>
            {{#each showtimes}}
            {{this}} |
            {{/each}}
        </div>
    </div>
</div>